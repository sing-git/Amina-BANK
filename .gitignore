# secrets — NEVER commit these
**/.env
**/.env.local
.env
.env.*
!**/.env.example

swisshacks/
# root-anchored: ignore the old top-level pipeline/ dir, NOT backend/src/pipeline/ source
/pipeline/

# node
node_modules/
dist/
build/
*.tsbuildinfo
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# os / editor
.DS_Store
.vscode/
.idea/

# Sanctions list source files (large, refreshed periodically — not committed)
# NOTE: root-anchored so it does NOT ignore source dirs like backend/src/data
# Parsed/indexed cache built from the raw source files
.cache/

# generated synthetic data (regenerated from generators; optional to keep)
backend/data/generated/*.json
__pycache__/
# Vite build cache (regenerated; never commit)
frontend/.vite/
**/.vite/

# Python venv for the pitch-deck generator (never commit)
**/.pptxvenv/
